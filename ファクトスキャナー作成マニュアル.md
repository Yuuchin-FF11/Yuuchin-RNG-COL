# ファクト・スキャナー作成・取扱マニュアル

本マニュアルは、ホームページ制作において画像サイズや音声リンク切れの不整合バグを自動検知し、ファイルの変更を常時自動で監視・反映する「ファクト・スキャナー」の構成、使い方、および設計について解説したドキュメントです。

---

## 1. ツールの概要と目的

ファクト・スキャナーは、ホームページのプログラムコード（HTML、CSS、JS）内に記述されたアセット（画像や音声）の設定と、実際のファイルの「物理サイズ」や「実在性」を照合し、表示ズレやリンク切れといった不整合バグを自動で検出する完全ローカル完結型の品質管理ツールです。

外部の有料APIやオンラインサーバーを一切使用せず、ユーザーのパソコン内（ローカル環境）だけで安全・高速に動作します。

---

## 2. システム構成とファイル配置

ツールは以下の3つのファイルで構成され、すべて「HP」フォルダの直下に配置されています。

### ・フロントエンドUI： [ファクトスキャナー起動.html](file:///c:/Users/user/OneDrive/デスクトップ/HP/ファクトスキャナー起動.html)
・ユーザーが操作するブラウザ画面です。HTML5、CSS3、およびバニラJavaScriptで実装されています。
・アセットのドロップ受付、画像解析、音声実在判定、および監視サーバーからの更新通知を受け取って画面を書き換える（ホットリロード）ロジックが組み込まれています。

### ・自動監視サーバー： [fact_monitor.py](file:///c:/Users/user/OneDrive/デスクトップ/HP/fact_monitor.py)
・Python 3 の標準ライブラリのみで動作する軽量なローカルWebサーバーです。
・起動すると「HP」フォルダ以下のファイルを監視し、更新日時（タイムスタンプ）に変化があった場合、フロントエンドに差分データと最新コードを送信するAPI（/api/status）を提供します。

### ・一発起動スクリプト： [監視サーバー起動.bat](file:///c:/Users/user/OneDrive/デスクトップ/HP/監視サーバー起動.bat)
・ダブルクリックするだけで、ポート8080の重複プロセス強制終了、Python環境の自動探索、監視サーバーの非同期起動、およびブラウザの自動立ち上げまでを全自動で行うWindows用バッチファイルです。
・文字化けや構文エラーを防ぐため、Shift-JIS/CRLFかつASCII文字（英語メッセージ）のみで構成されています。

---

## 3. 事前準備と起動方法

### ・事前準備（Pythonのインストール）
本ツールを使用するには、パソコンに本物のPythonがインストールされている必要があります。
インストールされていない場合は、コマンドプロンプトやPowerShellで以下のコマンドを実行してインストールしてください。

`winget install Python.Python.3.12`

### ・起動手順
1. 「HP」フォルダ直下にある「監視サーバー起動.bat」をダブルクリックします。
2. サーバー用の黒い画面が新しく開き、自動的にブラウザが立ち上がって「http://localhost:8080/」のツール画面が開きます。
3. 画面の右上に緑色で「自動監視：接続完了 (HP)」と点滅表示されていれば、起動成功です。

---

## 4. 機能一覧と操作手順

### ・ステップ1：検証するコードファイルの投入
・検証したいHTML、CSS、またはJSファイルを、左上の「【ステップ1】プログラムファイルをドロップ」エリアにドラッグ＆ドロップします。
・コード内から検出された画像や音声アセットのパスが抽出され、右側のレポートに一覧表示されます。

### ・ステップ2：アセットデータの投入
・実際の画像ファイル（PNG、JPG、JPEG、GIF）や音声ファイル（MP3、WAV、OGG）を、左下の「【ステップ2】アセット（画像や音声）をドロップ」エリアにドラッグ＆ドロップします（複数ファイルの一括ドロップが可能です）。

### ・画像サイズ整合性の自動検証
ドロップされた画像について、実際の画像サイズ（物理）とコード上の指定サイズを以下の3つの基準で自動比較します。
・HTML属性（width, height）
・CSSスタイル（外部CSSファイルのwidth, height）
・HTMLインラインスタイル（style属性内の指定）

指定が物理サイズを超えている場合は「致命的乖離（バグの危険）」として赤い警告カードが出力され、縦横比（アスペクト比）が崩れている場合は黄色の注意カードが出力されます。

### ・音声アセットの実在チェック（リンク切れ検出）
・コード内に記述された音声ファイルについて、ステップ2で実データが投入されているかをチェックします。
・投入されていない音声は「リンク切れの疑い（赤）」、投入された音声は「OK（緑）」としてレポートに表示されます。

### ・AI修正指示テキストの自動生成とコピー
・不整合が検出された各カードには「AIへの修正指示テキストをコピーする」ボタンが配置されています。
・クリックすると、実際の画像サイズ、コード上の指定サイズ、検出元の情報を含んだ論理的な修正指示テキストがクリップボードにコピーされます。これをAI（ChatGPTやGemini等）にそのまま貼り付けるだけで、修正コードを瞬時に得ることができます。

### ・常時自動監視（ホットリロード）
・ツール画面を開いた状態で、テキストエディタ（メモ帳やVS Codeなど）で読み込ませているコードファイルを編集し、上書き保存（Ctrl + S）します。
・サーバーが変更を自動検知し、ブラウザ側がドラッグ＆ドロップし直すことなくリアルタイムに検証レポートを自動更新します。

---

## 5. カスタマイズと設定変更方法

開発者向けに、動作仕様を調整するための変更箇所を解説します。

### ・監視対象外フォルダの追加・変更
「fact_monitor.py」の18〜22行目にある「EXCLUDE_DIRS」を変更することで、特定のフォルダを監視対象外に指定できます。初期状態では「.git」「.agent」「.agents」「.vscode」「scratch」が除外されています。
（※ただし、「scratch/test_bug.html」については、テストの利便性のためコード上で特別に監視許可を与える例外処理を組み込んでいます）

### ・ポート番号の変更
初期状態では「8080」ポートを使用しています。他のアプリケーションと衝突する場合は、以下の箇所の数値を変更してください。
・「fact_monitor.py」の15行目: `PORT = 8080`
・「監視サーバー起動.bat」の10行目: `:8080`
・「監視サーバー起動.bat」の35行目: `:8080/`
